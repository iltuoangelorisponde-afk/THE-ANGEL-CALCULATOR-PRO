import React from 'react';
import ReactDOM from 'react-dom/client';
import AngelCalculator from './AngelCalculatorPro';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AngelCalculator />
  </React.StrictMode>
);
